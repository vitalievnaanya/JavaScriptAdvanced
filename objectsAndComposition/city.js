function city(name, population, tressary) {
    const result = {
        name: name,
        population: population,
        tressary: tressary
    };

    return result
}

console.log(city('Tortuga', 7000, 15000))